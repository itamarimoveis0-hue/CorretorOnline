# Server Configuration
PORT=5000
NODE_ENV=development

# Database (optional - uses in-memory storage if not configured)
# For PostgreSQL with Railway:
# DATABASE_URL=postgresql://user:password@host:port/database
